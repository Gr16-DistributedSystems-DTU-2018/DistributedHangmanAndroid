<resources>

    <!-- Base application theme. -->
    <style name="SuperDarkTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:navigationBarColor">@color/primaryColor</item>
        <item name="android:statusBarColor">@color/primaryColor</item>
        <item name="android:colorBackground">@color/colorPrimary</item>
        <item name="colorPrimary">@color/colorPrimary</item>
    </style>

    <style name="SuperDarkTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="SuperDarkTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

</resources>